/**
 * Web interactive whiteboard plugin, node half.
 *
 * Deliberately empty: this package owns only the browser-side canvas panel.
 * Nothing model-facing registers here.
 */

/** Host plugin body — the browser half owns the canvas panel. */
export function apply(): void {}
